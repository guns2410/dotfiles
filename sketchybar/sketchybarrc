# This is a demo config to showcase some of the most important commands.
# It is meant to be changed and configured, as it is intentionally kept sparse.
# For a (much) more advanced configuration example see my dotfiles:
# https://github.com/FelixKratz/dotfiles

#!/usr/bin/env bash

# Ensure CONFIG_DIR is set when launched from different environments
if [ -z "$CONFIG_DIR" ]; then
  CONFIG_DIR="$HOME/.config/sketchybar"
fi

PLUGIN_DIR="$CONFIG_DIR/plugins"

# Resolve Aerospace binary explicitly to avoid PATH issues for LaunchAgents
AEROSPACE_BIN="$HOME/.config/aerospace/aerospace"
if [ ! -x "$AEROSPACE_BIN" ]; then
  # Fallback to PATH if not found at the default location
  AEROSPACE_BIN="aerospace"
fi

FONT="JetBrainsMono Nerd Font"
ICON_FONT="$FONT"
ITEMS_DIR="$CONFIG_DIR/items"

source "$CONFIG_DIR/icons.sh"
source "$CONFIG_DIR/colors.sh"

##### Bar Appearance #####
# Configuring the general appearance of the bar.
# These are only some of the options available. For all options see:
# https://felixkratz.github.io/SketchyBar/config/bar
# If you are looking for other colors, see the color picker:
# https://felixkratz.github.io/SketchyBar/config/tricks#color-picker

bar=(
    padding_left=0
    padding_right=0
    position=top
    height=30
    blur_radius=30
    corner_radius=8
    margin=12
    y_offset=2
    shadow=true
    color=$BASE
)
sketchybar --bar "${bar[@]}"

##### Changing Defaults #####
# We now change some default values, which are applied to all further items.
# For a full list of all available item properties see:
# https://felixkratz.github.io/SketchyBar/config/items

default=(
  padding_left=5
  padding_right=5
  icon.font="$FONT:Bold:17.0"
  label.font="$FONT:Bold:14.0"
  icon.color=$WHITE
  label.color=$WHITE
  icon.padding_left=4
  icon.padding_right=4
  label.padding_left=4
  label.padding_right=4
  color=$BASE
)
sketchybar --default "${default[@]}"

##### Workspaces (AeroSpace) #####
# Remove Mission Control/Yabai spaces and drive spaces from AeroSpace instead.
# Add a custom event and create one item per AeroSpace workspace.
sketchybar --add event aerospace_workspace_change

source "$ITEMS_DIR/apple.sh"
source "$ITEMS_DIR/front_app.sh"
source "$ITEMS_DIR/spaces.sh"


##### Adding Right Items #####
source "$ITEMS_DIR/ip.sh"
source "$ITEMS_DIR/clock.sh"
source "$ITEMS_DIR/calendar.sh"
source "$ITEMS_DIR/battery.sh"
source "$ITEMS_DIR/sound.sh"

##### Force all scripts to run the first time (never do this in a script) #####
sketchybar --update
